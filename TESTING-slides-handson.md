# caw ハンズオン手順書 — スライド作成（第 2 回テスト会・Windows 版）

第 2 回テスト会では、**caw で研究発表スライド（PowerPoint）を作る**のを体験してもらいます。
自分の実データ画像や論文の図を使って、その場でスライドを 1 セット作るのがゴールです。

- 対象 OS：**Windows のみ**
- 使う CLI：**Claude Code**（第 1 回でオンボーディング済みの想定。まだの人は最後の補足を参照）
- 所要：ツール導入 10〜15 分＋作成体験 20〜30 分

---

## この会でやること（全体像）

1. **スライド用のツールを入れる**（caw に「環境を整えて」と頼めば、必要なものを 1 つずつ説明して入れてくれます）
2. **自分の画像を用意して**、「スライド作って」と頼む
3. できた **`.pptx` を PowerPoint で開いて**、図形・表・数式・自分の画像が編集できることを確認する
4. 感想・不具合をフィードバックする

> caw のスライドは **手描き SVG → 編集可能な PowerPoint（native な図形・表・グラフ）** に変換して作ります。画像は貼るだけでなく、表やグラフ・数式も「あとから PowerPoint で直せる」形で入ります。

---

## 事前準備（当日までに用意してほしいもの）

どれか 1 つあれば体験できます（両方あるとより楽しめます）。

- **自分の実験・解析画像**（顕微鏡写真・装置スクショ・XRD/DSC のプロット・スキャンした手描き図など）を 1〜3 枚。PNG / JPEG。
- **紹介したい論文の PDF** を 1 本（論文紹介スライドを作る場合）。

第 1 回でセットアップした **プロジェクトフォルダ**（`office/` と `work/` がある場所）を使います。
新しく別テーマで試したい人は、空フォルダを作って Claude Code を起動し `/caw` からで OK です。

---

## Phase 1 — スライド用ツールを入れる（Windows）

スライド生成には Python の追加パッケージがいくつか要ります。**caw に任せるのが一番簡単**です。

### 方法 A（推奨）— caw に入れてもらう

Claude Code（プロジェクトフォルダで起動）で、こう頼むだけ：

```
環境を整えて
```

すると caw が、**不足しているツールを 1 つずつ「なぜ必要か」を説明しながら**「入れますか？」と聞いてきます。スライドに関係するのは次の 4 つです（すでに入っていれば聞かれません）：

| ツール | なぜ必要か |
|---|---|
| **python-pptx** | 手描き SVG を**編集可能な pptx** に変換する（スライドの核） |
| **Pillow** | 写真・切り抜き図（画像）をスライドに埋め込む |
| **PyMuPDF** | 論文 PDF から図を高解像度で切り抜く |
| **matplotlib** | 数式（LaTeX）のレンダ・グラフ作成 |

「はい」で進めれば、`py -m pip install ...` を順に実行してくれます（管理者権限は不要）。

### 方法 B — 手動で入れる（自分でやりたい人）

PowerShell で：

```powershell
py -m pip install python-pptx pillow pymupdf matplotlib numpy
```

論文 PDF の図抽出まで使うなら poppler も（任意）：

```powershell
scoop install poppler        # Scoop 未導入なら https://scoop.sh/ を先に
```

### 入ったか確認

```powershell
python -c "import pptx, PIL, fitz, matplotlib; print('slide deps OK')"
```

`slide deps OK` と出れば準備完了です。

> **日本語フォントについて**：Windows には **MS Gothic / Yu Gothic** が標準で入っているので、追加インストールは不要です（caw のスライドは和文＝MS Gothic を前提にしています）。

---

## Phase 2 — スライドを作る（ハンズオン本編）

### Step 1. 使いたい画像をフォルダに置く

プロジェクトの **`work/presentations/figures/`** に、用意した画像を入れます。
（第 1 回のオンボーディングで自動作成されています。無ければ「スライド用の figures フォルダ作って」でも OK）

- ファイル名は**内容が分かる名前**に（例：`xrd_120C.png`、`microscope_sample.jpg`）。
- ここに置いた画像を caw が拾って、**アスペクト比を保って**スライドに埋め込みます。

### Step 2. 「スライド作って」と頼む

Claude Code で、作りたいものを伝えます。例：

- 報告会向け：**「今週の進捗を報告会スライドにして。`work/presentations/figures/` の画像を使って」**
- 論文紹介：**「この論文を紹介するスライドを作って」**（論文 PDF を `work/papers/pdf/` に入れて「登録して」→「紹介して」でも可）
- 数式を入れたい：**「アレニウスの式 `k = A e^{-Ea/RT}` を入れて」**（数式は自動で図としてきれいに入ります）

caw が中で、SVG を描く → 豆腐（□）や重なりを自動チェック → **編集可能な pptx に変換** → 画像の埋め込み・フォント修正、まで一気にやります。**手でコマンドを打つ必要はありません。**

### Step 3. できた pptx を PowerPoint で開く

出力先は **`work/presentations/slides/<名前>.pptx`** です。エクスプローラーからダブルクリックで開きます。

確認してほしいポイント：

- **図形・矢印・表が「画像」ではなく編集できる**（クリックして色や文字を直せる）
- **グラフが native**（PowerPoint 上で数値を直せる）
- **数式**がきれいに入っている
- **`figures/` に置いた自分の画像**が、潰れずに（縦横比を保って）入っている
- タイトルが主張形になっている・1 枚 1 メッセージになっている

気に入らなければ、そのまま **「3 枚目の図を大きく」「タイトルを変えて」** のように頼めば作り直してくれます。

---

## Phase 3 — フィードバック

以下を教えてください（口頭・チャットどちらでも）：

- ツール導入（Phase 1）で詰まったところ
- 「スライド作って」で**思ったものと違った**点／良かった点
- PowerPoint で開いたときの**編集しやすさ**
- 自分の画像・数式・論文図の**入り具合**
- 「こういう用途でも使いたい」という要望

---

## うまくいかない時（Windows トラブルシュート）

| 症状 | 対処 |
|---|---|
| `python` が見つからない | `py` を使う（`py -m pip ...` / `py --version`）。インストール時に「Add python.exe to PATH」を有効にしていないと出ます |
| `import pptx` 等でエラー | Phase 1 の `py -m pip install ...` をやり直す。仮想環境を使っている場合はそれを有効化してから |
| スライドの日本語が □（豆腐）になる | まれに起きます。その旨を caw に伝えれば `fix_ea_font` で直します（和文を MS Gothic に強制） |
| 図が「画像」になっていて編集できない | 写真・論文の切り抜き図は画像のままです（それが正常）。表・グラフ・矢印図は native な図形で入ります |
| 論文 PDF から図が切り抜けない | PyMuPDF（Phase 1）が入っているか確認。poppler も入れると PDF 処理が安定します |
| `.pptx` がどこにできたか分からない | `work/presentations/slides/` を見る。caw が最後にパスを教えてくれます |

---

## 補足：まだ caw を入れていない人（第 1 回未参加）

1. 土台と AI CLI を入れる（詳細は Web の「必要なツールとインストール」ページ）：
   ```powershell
   winget install OpenJS.NodeJS.LTS
   winget install Git.Git
   winget install Python.Python.3.12
   npm install -g @anthropic-ai/claude-code
   ```
   ※ Claude Code の Hooks は **Git Bash**（`Git.Git` 同梱）を使います。
2. プロジェクト用の空フォルダで `claude` を起動 → `/caw` でオンボーディング（研究分野などを聞かれます）。
3. その流れで Phase 1 のツール導入も聞かれるので「はい」で進める。
4. あとは Phase 2 へ。

---

**この手順書の対象バージョン**：caw plugin v1.70 以降（SVG-first スライド・per-tool ツール導入・`work/presentations/figures/` 対応）。
インストールの詳細は Web ドキュメント「必要なツールとインストール」（`web/.../requirements.md`）も参照。
