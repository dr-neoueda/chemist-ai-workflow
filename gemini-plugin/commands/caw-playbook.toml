description = "計算ノウハウを Playbook に蓄積／過去データを取り込む（研究）"
prompt = "GEMINI.md の『caw-playbook』手順に従う。計算で得た知見を office/computation/playbooks/<tool>.md の Lessons Learned に日本語で末尾追記。既定を変える教訓なら『デフォルト推奨パラメータ』ブロックも更新。計算ソフトの _past-data/ があれば解析して既定傾向を seed。ソフトを超えた一般則は memory（Claude）か computation の共通知見（他 CLI）に。{{args}}"
