---
name: caw-paper
description: >
  入手済み PDF からメタデータを抽出し、書誌情報付き要約 md を生成して
  ナレッジベース + クラウドストレージへ自動登録する「論文ライブラリ管理」スキル（研究トラック）。発動時に抽出レベル（L1 軽量／L2 中間／L3 は paper-register 相当のフル抽出、深いほどトークン消費が増える）を必ず尋ねる。論文の検索・探索は caw-research が担当。
trigger: /caw-paper
---

# caw-paper — 論文の登録・管理（ライブラリ）

## いつ使うか

- `/caw-paper` を実行したとき
- `work/papers/` に PDF を置いてユーザーが「論文を登録して」「PDF を取り込んで」と言ったとき
- 既存の `work/papers/` に書誌情報を追加したいとき

**論文の検索・探索（「○○ について論文を集めて」「関心テーマの文献を調べて」等）は `caw-research`（研究トラック）の担当**。caw-research が `work/topics/` に作ったリストから、ユーザーが PDF を取得して `work/papers/` に置いたものを、本スキルが登録する。`office/research/` が無ければ `/caw` で research 部署を追加するよう促す。

---

## はじめてモードを尊重する

このスキルを実行する前に `office/CLAUDE.md`（Codex CLI / GitHub Copilot CLI では `AGENTS.md`）を読み、冒頭に `> 運用モード: はじめて` があれば、`caw` skill の「はじめてモードの挙動」を全応答に適用する：**平易な日本語**で話し、専門用語（化学・計算手法・書誌の用語）は初出で 1 行説明を添え、各ステップの最後に**「次はこれをしましょう」を 1 つ**だけ提示する。元に戻せない操作（削除・上書き・外部登録・送信）は必ず事前確認する。

## モード A: PDF 登録（入手済み PDF → ナレッジベース）

### 前提

- `work/papers/` ディレクトリに対象 PDF が配置されている（caw scaffold で自動生成済）
- `office/CLAUDE.md` の「オーナープロフィール」で **ナレッジベース** と **クラウドストレージ** が指定されている

### Step 0: 抽出レベルを尋ねる（必須・省略禁止）

**caw-paper を起動したら、登録を始める前に必ず `AskUserQuestion` で「抽出レベル」を尋ねる。** PDF やファイルだけ渡された場合でも、**自動でレベルを決めず必ず一度尋ねてから着手する**。AI の使用量（トークン）はこのレベルで大きく変わるため、ユーザーに選ばせる。

```
抽出レベル（深いほど AI 使用量〔トークン〕が増えます：L1 < L2 < L3）:
  - L1 — 書誌情報 + 要旨（4 行）+ 結論の要点。数値・手法・背景は載せない
  - L2 — L1 + One-line summary・背景概要・対象/手法の概要・主要な結果（代表テーブル＋ headline 数値）
  - L3 — フル抽出（背景・動機／対象物質・系／手法詳細／主要な結果〔全数値・数式・図表要点〕／考察／本研究の限界／関連研究との比較／引用文脈テンプレート／検索キーワード）＝ paper-register 相当
  (multiSelect: false; Other 可)
```

> **どのレベルも「推奨」にはしない**。ただし**レベルが上がるほど読む範囲・生成量が増え、AI 使用量（トークン）が増える（L1 < L2 < L3）こと**を明示して選んでもらう。**尋ねること自体は絶対に省略しない**（PDF だけ渡されても自動で決めない）。選んだレベルが **Step 2 の抽出**と **Step 3 の md の充実度**の両方を決める。**モード B（バッチ）では最初に 1 回だけ尋ね、全 PDF に同じレベルを適用**する。

### Step 1: PDF の検出

```bash
ls work/papers/*.pdf
```

新規追加された PDF を検出（既に `work/papers/` に登録済の md と照合してスキップ）。

### Step 2: メタデータ抽出（Step 0 のレベルで深さを変える）

各 PDF について、まず `pdftotext` でテキスト変換（`pdftotext "work/papers/<file>.pdf" /tmp/<file>.txt`）。以降は **Step 0 で選ばれたレベル**で抽出量を変える：

| レベル | 読む範囲 | 生成する md の中身 | タグ |
|---|---|---|---|
| **L1** | 頭 2 ページ＋要旨 | 書誌情報 + 要旨 4 行 + 結論の要点 | 3〜5 個 |
| **L2** | 要旨＋結論＋主要な図表 | L1 ＋ One-line summary・背景概要・対象/手法の概要・主要な結果（代表テーブル＋ headline 数値） | 5〜7 個 |
| **L3** | 本文全体（手法・結果・考察・参考文献まで） | フル構造化＝背景・動機／対象物質・系／手法（詳細）／主要な結果〔全数値・数式・図表要点〕／考察・著者の主張／本研究の限界／関連研究との比較／引用文脈テンプレート／キーワード（**paper-register 相当**） | 10+ 個 |

- **レベルが上がるほど読む本文範囲と生成量が増える＝AI 使用量が増える（L1 < L2 < L3）。L3 は paper-register スキルと同等の網羅的抽出。**
- どのレベルでも、`pdftotext` で読めない PDF（画像・OCR 未処理）はファイル名から推定してユーザーに確認する。大容量（>100 MB）は頭 10 ページのみ抽出。

### Step 3: ナレッジベース用 md 生成

`work/papers/<author-year-keyword>.md` に書誌情報付き md を生成。命名規則：
- `<first-author-lastname>-<year>-<keyword>.md`
- 例: `tanaka-2024-mof-luminescence.md`

**md のセクション構成は Step 0 のレベルに連動する**（下ほど上を包含）：
- **L1**: 書誌情報 / 要旨（4 行）/ 結論の要点 / ネクストアクション / 関連部署
- **L2**: L1 ＋ One-line summary / 背景・ねらい / 対象・手法（概要）/ 主要な結果（代表テーブル＋ headline 数値）
- **L3**: One-line summary / 背景・動機 / 対象物質・系 / 手法（詳細）/ 主要な結果（数値・数式・図表要点すべて）/ 考察・著者の主張 / 本研究の限界 / 関連研究との比較 / 引用文脈テンプレート / キーワード（検索用）/ ネクストアクション / 関連部署 ＝ **paper-register と同じ網羅度**

下の雛形は **L3（フル）** の構成。L1/L2 では上記の該当セクションのみを書く（L1 は One-line summary の代わりに 4 行の「要旨」を使う）。

フォーマット：

```markdown
---
title: "..."
authors:
  - "First Author"
  - "Second Author"
year: 2024
journal: "..."
volume: ...
pages: "..."
doi: "10.1021/..."
url: "https://doi.org/..."
pdf_local: "work/papers/<file>.pdf"
pdf_url: "TBD"
tags:
  - tag1
  - tag2
added: YYYY-MM-DD
status: "to-read"
---

# <Title>

## 書誌情報

- 著者: ...
- 掲載: ...
- DOI: ...

## One-line summary

（1 文で論文の核を要約。L2/L3。L1 では代わりに 4 行の「要旨」を置く）

## 背景・動機

（分野の課題・本研究のねらい。L3）

## 対象物質・系

（化合物・系・条件。L3）

## 手法（詳細）

（合成・測定・計算の手順と条件。L3）

## 主要な結果（数値・数式・図表要点すべて）

（テーブル・数値・式・図表の要点。L2 は代表値のみ、L3 は網羅）

## 考察・著者の主張

（著者の解釈・主張。L3）

## 本研究の限界

（L3）

## 関連研究との比較

（先行研究との位置づけ。L3）

## 引用文脈テンプレート

（[定義の根拠]／[実験的事実の根拠]／[手法の先行例]／[原理・理論の根拠] それぞれ引用しやすい一文。L3）

## キーワード（検索用）

（英日キーワード列挙。L3）

## ネクストアクション

- [ ] 読了
- [ ] 関連論文との比較
- [ ] 引用候補としてマーク

## 関連部署

- 文献部: `../`
- ナレッジベース: <ナレッジベース名>
- クラウドストレージ: <クラウドストレージ名>
```

### Step 4: ナレッジベースへの登録（オプション）

ユーザープロファイルの「ナレッジベース」設定に応じて：

#### Notion の場合

- MCP `notion-create-pages` を使用（権限がある場合）
- データソース ID をユーザーから取得（`office/CLAUDE.md` に保存されていれば使用、無ければ問い合わせ）
- frontmatter のフィールドを Notion プロパティにマッピング

#### Obsidian の場合

- MCP `obsidian-append-content` を使用、または vault パスをユーザーから取得して直接書き込み
- frontmatter をそのまま保持（Obsidian は YAML frontmatter ネイティブ対応）

#### Logseq / 他

- frontmatter 付き md ファイルをユーザー指定の vault ディレクトリにコピー

#### 「使わない / 未定」の場合

- `work/papers/` 配下のローカル md のみで完結
- 将来的な KB 連携のため frontmatter は維持

### Step 5: クラウドストレージへのアップロード（オプション）

「クラウドストレージ」設定に応じて：

#### Google Drive

- MCP `google-drive-create-file` を使用
- アップロード後の URL を取得し、登録済 md の `pdf_url` を実 URL に更新

#### Dropbox / OneDrive

- 現状は手動アップロードを促す（自動化は将来対応）

#### 「使わない / ローカルのみ」

- スキップ

### Step 6: 登録レポート

完了後、以下をユーザーに報告：

```
登録完了：

| ファイル | 登録先 md | KB 登録 | クラウドアップロード |
|---|---|---|---|
| paper1.pdf | tanaka-2024-mof.md | ✅ Notion | ✅ Drive |
| paper2.pdf | yamada-2023-xrd.md | ✅ Notion | ⏳ 手動アップロード待ち |

スキップ件数: 2 件（既登録）
失敗件数: 0 件
```

---

## モード B: バッチ処理（複数 PDF 一括）

`/caw-paper --batch` でユーザーが起動した場合：

- 最初に **Step 0（抽出レベル）を 1 回だけ尋ね**、`work/papers/` 配下の全 PDF を対象に同じレベルで Step 1〜6 を順次実行
- 1 ファイルあたり 30-60 秒程度を想定（PDF サイズによる）
- 進捗を逐次表示（"3/10 件処理中..."）
- 失敗ファイルはスキップして続行、最後にまとめて報告

---

## 重要な注意事項

- **抽出レベル（L1/L2/L3）は毎回必ず `AskUserQuestion` で尋ねる**（PDF だけ渡されても自動で決めない・絶対）。推奨は設けず、**レベルでトークン使用量が変わる（L1 < L2 < L3）ことを明示**して選ばせる。バッチは最初に 1 回だけ尋ねる
- **要約・抽出した本文はユーザーの言語（既定で日本語）で書く**（原文が英語でも日本語に要約する）
- **既存ファイルは絶対に上書きしない**。同名 md があれば skip して報告
- **PDF 自動ダウンロード機能は未対応**（論文検索は `caw-research` が担当。そのリストからユーザーが手動で PDF を取得し `work/papers/` に置く前提）
- **メタデータ抽出の精度**：pdftotext で読めない PDF（画像 PDF・OCR 未処理）は、ファイル名から推定 + ユーザーに確認を求める
- **化学物質名・反応名の正確性**：自動タグ付けは初期案として提示、ユーザーに確認・修正の機会を与える
- **大容量 PDF（>100 MB）**：pdftotext で頭 10 ページのみ抽出、フォールバック処理
- **DOI 重複チェック**：同じ DOI の論文を 2 回登録しないよう、frontmatter の `doi` フィールドで照合
